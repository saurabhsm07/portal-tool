import { MapStatusPipe } from './map-status.pipe';

describe('MapStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MapStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
