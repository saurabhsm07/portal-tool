import { MapPriorityPipe } from './map-priority.pipe';

describe('MapPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new MapPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
