import { SearchTicketPipe } from './search-ticket.pipe';

describe('SearchTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
