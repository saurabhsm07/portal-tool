import { SearchSectionPipe } from './search-section.pipe';

describe('SearchSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
