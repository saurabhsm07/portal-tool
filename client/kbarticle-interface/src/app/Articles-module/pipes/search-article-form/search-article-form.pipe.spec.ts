import { SearchArticleFormPipe } from './search-article-form.pipe';

describe('SearchArticleFormPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArticleFormPipe();
    expect(pipe).toBeTruthy();
  });
});
