import { SearchArticlePipe } from './search-article.pipe';

describe('SearchArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
