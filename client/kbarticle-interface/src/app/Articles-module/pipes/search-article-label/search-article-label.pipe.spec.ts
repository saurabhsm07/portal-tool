import { SearchArticleLabelPipe } from './search-article-label.pipe';

describe('SearchArticleLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArticleLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
