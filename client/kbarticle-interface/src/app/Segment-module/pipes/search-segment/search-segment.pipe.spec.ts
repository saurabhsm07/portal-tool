import { SearchSegmentPipe } from './search-segment.pipe';

describe('SearchSegmentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSegmentPipe();
    expect(pipe).toBeTruthy();
  });
});
